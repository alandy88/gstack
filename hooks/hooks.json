{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "mkdir -p .claude/gstack && touch .claude/gstack/learnings.jsonl",
        "timeout": 5000
      }
    ]
  }
}
